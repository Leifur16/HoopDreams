/*
TODO add:
playedGames : An array of PickupGame (where the array cannot be null nor the
items within the array
*/

module.exports = `
    type Player {
        id: ID!
        name: String!
    }
`;
